name: inputs demo
on:
  workflow_dispatch:
    inputs:
      environment: 
        description: choose env to deploy  
        required: true
        default: dev
jobs:
  input-demo:
    runs-on: ubuntu-latest
    steps:
        - name: deploy the app
          run: echo "deploy the app ${{inputs.environment}}"
